<VirtualHost *:80>
    ServerName yourdomain.com

    # Redirect HTTP to HTTPS (Requires mod_rewrite)
    # RewriteEngine On
    # RewriteCond %{HTTPS} off
    # RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R=301,L]

    # Security: Harden Apache
    ServerTokens Prod
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-Content-Type-Options "nosniff"
    Header set Referrer-Policy "strict-origin-when-cross-origin"

    # Security: Basic Auth
    <Location />
        AuthType Basic
        AuthName "Restricted Access"
        AuthUserFile /etc/apache2/.htpasswd
        Require valid-user
    </Location>

    # Proxy settings
    ProxyRequests Off
    ProxyPreserveHost On

    # Streaming optimizations for Ollama
    <Location /ollama/>
        # Disable proxy buffering for streaming
        SetEnv proxy-nokeepalive 1
        SetEnv proxy-sendchunked 1
    </Location>

    # Proxy Ollama
    # Note: Trailing slashes are important for path stripping
    # Security: Prevent Origin leakage to Ollama (removes need for OLLAMA_ORIGINS=*)
    RequestHeader set Origin ""
    ProxyPass /ollama/ http://127.0.0.1:11434/
    ProxyPassReverse /ollama/ http://127.0.0.1:11434/

    # Proxy OpenClaw
    ProxyPass / http://127.0.0.1:3000/
    ProxyPassReverse / http://127.0.0.1:3000/
</VirtualHost>
